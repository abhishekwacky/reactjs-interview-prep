# react-router-dom

- It enables "client side routing"
- Basically In react pages are rendered dynamically using component. react-router intersecepts the browser request and injects the components contents. which helps us in SPA behaviour for faster rendering without the page refresh's.
